extends layouts/frontend

block title
    title TP Node S2 - Home

block content
    .ui.middle.aligned.center.aligned.grid
        .six.wide.column
            h2 Inscrivez-vous
            form.ui.large.form(method="post", action="/")
                .ui.stacked.segment
                    +field('text', 'firstName', 'Prénom')
                    +field('text', 'lastName', 'Nom de famille')
                    +field('email', 'email', 'votre.email@mail.com')
                    +field('password', 'password', 'Mot de passe')
                    +field('password', 'confirmPassword', 'Confirmer le mot de passe')
                    button(type="submit").ui.fluid.large.teal.submit.button M'inscrire

                .ui.error.message


mixin field(type, name, placeholder)
    .field
        .ui.left.icon.input
            i.icon(class=(type === 'password' ? 'lock' : 'user'))
            input(type=type, name=name, placeholder=placeholder)
